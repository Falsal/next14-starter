export {/* @next-codemod-ignore `GET` export is re-exported. Check if this component uses `params` or `searchParams`*/
GET, /* @next-codemod-ignore `POST` export is re-exported. Check if this component uses `params` or `searchParams`*/
POST} from "@/lib/auth"

// when building replace error with ignore after making sure all checks have been completed